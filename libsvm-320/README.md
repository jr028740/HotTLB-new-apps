# SVM Evaluation
## 1. Uncompress `svm_data.tar.gz` to get the data file. It requires additional 5.7GB disk space.
## 2. For `[system]-svm.sh`, the `perf-scripts` folder and the pmdmon scripts are defaulted to save at home folder. Change them accordingly if they are saved in other places.
