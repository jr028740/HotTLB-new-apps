# CC Evaluation
## 1. Change the PERF\_PATH in `perf_command.sh`to your perf executable
## 2. In `[system]-cc.sh`, change the `cd /path/to/gapbs-1.5` to the path of the saved `gapbs-1.5` workload directory, and change the `/path/to/pmdmon_XXX` to the directory saving the pmdmon scripts.
